{
  "name": "odin-restaurant-page",
  "version": "1.0.0",
  "description": "The Odin Project Restaurant Page Project.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Xavier Tondelier",
  "license": "ISC",
  "devDependencies": {
    "css-loader": "^7.1.2",
    "html-loader": "^5.1.0",
    "html-webpack-plugin": "^5.6.4",
    "style-loader": "^4.0.0",
    "webpack": "^5.102.1",
    "webpack-cli": "^6.0.1",
    "webpack-dev-server": "^5.2.2",
    "webpack-merge": "^6.0.1"
  },
  "scripts": {
    "build": "webpack --config webpack.prod.js",
    "dev": "webpack serve --config webpack.dev.js",
    "deploy": "git checkout gh-pages && git merge --no-edit main && git add -f dist && git commit -m'Deployment commit' && git subtree push --prefix dist origin gh-pages && git checkout main",

    "prebuild": "echo 'Building project in production mode'",
    "predev": "echo 'Initialising dev server'"
  }
}
